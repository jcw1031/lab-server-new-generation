spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lab_service?characterEncoding=UTF-8&serverTimezone=UTC
    username: jcw
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        highlight_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace
    org.apache.coyote.http11: debug
    knu.networksecuritylab.appserver: info

jwt:
  secret: KNU_NETWORK_SECURITY_LAB

server:
  port: 9150